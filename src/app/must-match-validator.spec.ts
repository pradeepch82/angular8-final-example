import { MustMatchValidator } from './must-match-validator';

describe('MustMatchValidator', () => {
  it('should create an instance', () => {
    expect(new MustMatchValidator()).toBeTruthy();
  });
});
